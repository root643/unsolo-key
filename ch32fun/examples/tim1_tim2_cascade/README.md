# Demonstration of using Timer 1 to trigger Timer 2
This example uses Timer 1 to count up every 1ms until 1000 and then clock Timer 2 which then counts seconds.

## Use
The program will output a timestamp every 500ms +/- a small amount.
